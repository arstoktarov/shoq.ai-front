import {makeStyles} from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    formControl: {
        minWidth: 150,
    },
    infoText: {
        textIndent: "50px",
        fontFamily: "Roboto",
        fontWeight: 400,
        fontSize: "16px",
        color: "#1C242C",
    },
    table: {
        boxShadow: "none",
    },
}));